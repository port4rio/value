import { ColumnDefinition } from '../types';

export const STOCK_COLUMNS: ColumnDefinition[] = [
  {
    key: 'code',
    label: 'コード',
    unit: '',
    tooltip: '証券コード',
    align: 'center',
    minWidth: 'w-[54px] min-w-[54px] max-w-[54px]',
    priority: 'high',
    format: (v) => (v != null ? String(v) : '-'),
  },
  {
    key: 'name',
    label: '銘柄名',
    unit: '',
    tooltip: '企業名',
    align: 'left',
    minWidth: 'w-[112px] min-w-[112px] max-w-[112px]',
    priority: 'high',
    format: (v) => (v != null ? String(v) : '-'),
  },
  {
    key: 'stayDays',
    label: '滞在日数',
    tooltip: 'スクリーニング在籍日数（居残り90日以上 / 転入30日以内）',
    align: 'right',
    minWidth: 'w-[72px] min-w-[72px]',
    priority: 'high',
    format: (v, stock) => {
      if (stock?.category === 'sotsugyo') return '卒業';
      return v != null ? `${v}日` : '-';
    },
  },
  {
    key: 'close',
    label: '現在値',
    unit: '円',
    tooltip: '直近取引値（円）',
    align: 'right',
    minWidth: 'min-w-[80px]',
    priority: 'high',
    format: (v) => (v != null ? `${Number(v).toLocaleString()}円` : '-'),
  },
  {
    key: 'change',
    label: '前日比',
    unit: '%',
    tooltip: '前営業日比の騰落率（%）',
    align: 'right',
    minWidth: 'min-w-[75px]',
    priority: 'high',
    format: (v) => {
      if (v == null) return '-';
      const num = Number(v);
      return `${num > 0 ? '+' : ''}${num.toFixed(2)}%`;
    },
  },
  {
    key: 'market_cap',
    label: '時価総額',
    unit: '億',
    tooltip: '時価総額（億円単位）',
    align: 'right',
    minWidth: 'min-w-[80px]',
    priority: 'medium',
    format: (v) => (v != null ? `${Number(v).toLocaleString()}億` : '-'),
  },
  {
    key: 'dividend_yield',
    label: '配当利回り',
    unit: '%',
    tooltip: '最新予想配当利回り（基準: 4.0%以上）',
    align: 'right',
    minWidth: 'min-w-[85px]',
    priority: 'high',
    format: (v) => (v != null ? `${Number(v).toFixed(2)}%` : '-'),
  },
  {
    key: 'payout_ratio',
    label: '配当性向',
    unit: '%',
    tooltip: '通期配当性向（純利益に対する配当金の割合）',
    align: 'right',
    minWidth: 'min-w-[85px]',
    priority: 'medium',
    format: (v) => (v != null ? `${Number(v).toFixed(1)}%` : '-'),
  },
  {
    key: 'per',
    label: 'PER',
    unit: '倍',
    tooltip: '最新予想PER（参考指標）',
    align: 'right',
    minWidth: 'min-w-[68px]',
    priority: 'high',
    format: (v) => (v != null ? `${Number(v).toFixed(2)}倍` : '-'),
  },
  {
    key: 'pbr',
    label: 'PBR',
    unit: '倍',
    tooltip: '株価純資産倍率（基準: 1.0倍以下 / 資産割安）',
    align: 'right',
    minWidth: 'min-w-[68px]',
    priority: 'high',
    format: (v) => (v != null ? `${Number(v).toFixed(2)}倍` : '-'),
  },
  {
    key: 'roe',
    label: 'ROE',
    unit: '%',
    tooltip: '自己資本利益率（基準: 8.0%以上 / 資本効率）',
    align: 'right',
    minWidth: 'min-w-[72px]',
    priority: 'high',
    format: (v) => (v != null ? `${Number(v).toFixed(2)}%` : '-'),
  },
  {
    key: 'ebitda_growth',
    label: 'EBITDA成長率',
    unit: '%',
    tooltip: '通期EBITDAの前年比成長率 (10%以上は優良)',
    align: 'right',
    minWidth: 'min-w-[105px]',
    priority: 'medium',
    format: (v) => {
      if (v == null) return '-';
      const num = Number(v);
      return `${num > 0 ? '+' : ''}${num.toFixed(2)}%`;
    },
  },
  {
    key: 'equity_ratio',
    label: '自己資本比率',
    unit: '%',
    tooltip: '自己資本比率（(総資産 - 総負債)/総資産 / 基準: 50%以上）',
    align: 'right',
    minWidth: 'min-w-[90px]',
    priority: 'high',
    format: (v) => (v != null ? `${Number(v).toFixed(2)}%` : '-'),
  },
  {
    key: 'de_ratio',
    label: 'D/Eレシオ',
    unit: '倍',
    tooltip: '負債資本倍率（有利子負債 / 株主資本） (1倍以下は健全、0.5倍以下は優良)',
    align: 'right',
    minWidth: 'min-w-[80px]',
    priority: 'low',
    format: (v) => (v != null ? `${Number(v).toFixed(2)}倍` : '-'),
  },
  {
    key: 'current_ratio',
    label: '流動比率',
    unit: '%',
    tooltip: '流動資産 ÷ 流動負債（短期支払能力） (150%以上は優良)',
    align: 'right',
    minWidth: 'min-w-[80px]',
    priority: 'low',
    format: (v) => (v != null ? `${Number(v).toFixed(1)}%` : '-'),
  },
];
